<Window x:Class="NetworkMon.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        WindowStyle="None"
        WindowStartupLocation="Manual"
        Left="500" Top="0"
        Topmost="True"
        ShowInTaskbar="False"
        AllowsTransparency="True"  Background="Transparent"
        Title="MainWindow" Height="65" Width="280">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="lbl" Grid.Row="1"
                   Style="{DynamicResource BaseTextBlockStyle}"
                   FontSize="20px" TextAlignment="Center" Foreground="White" Opacity="0.5">2 MB/s (500 KB/s)</TextBlock>
        <TextBlock x:Name="lblUp" 
                   Grid.Row="0"
                   Style="{DynamicResource BaseTextBlockStyle}"
                   FontSize="20px" TextAlignment="Center" Foreground="White" Opacity="0.5">0 KB/s</TextBlock>

    </Grid>
</Window>
